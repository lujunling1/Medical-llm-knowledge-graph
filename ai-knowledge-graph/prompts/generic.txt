[system]
You are a knowledge graph extraction assistant.
Your task is to extract concise Subject-Predicate-Object triples from user-provided text.
Return valid JSON. Exclude explanations, Markdown, and extra text.

[user]
Extract Subject-Predicate-Object knowledge graph triples from the text below.

Rules:
- Return a JSON array.
- Each item must be an object with exactly these keys: "subject", "predicate", "object".
- Use concise entity names for subjects and objects.
- Predicates must be short relationship phrases, preferably 1 to 3 words.
- Do not invent information that is not stated or strongly implied by the text.
- Merge obvious aliases into one consistent name when the text clearly refers to the same entity.
- Skip vague triples that do not add useful knowledge.

Expected JSON shape:
[
  {
    "subject": "entity A",
    "predicate": "relates to",
    "object": "entity B"
  }
]

Text:
<<<
{text}
>>>
